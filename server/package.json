{
  "name": "server",
  "version": "1.0.0",
  "description": "prime days server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development nodemon ./bin/www.js",
    "start": "npm run clean && npm run compile && node ./bin/www.js",
    "build": "npm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2",
    "rxjs": "^5.3.0",
    "simple-ngram-markov": "^1.0.4",
    "twitter-node-client": "0.0.6"
  },
  "devDependencies": {
    "@types/express": "^4.0.35",
    "rimraf": "^2.6.1",
    "typescript": "^2.2.2"
  }
}
